<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>book now</title>
    <!-- <link rel="stylesheet" type="text/css" href="edit.css"> -->
    <style>
        body {
            background-color: #f2AA4CFF !important;

        }

        #myform {
            display: flex;
            justify-content: center;
            margin: 10px;
            width: 400px;
            border: 1px solid yellow;
            /* filter: drop-shadow(4px 4px 5px rgb(252, 248, 244)); */
            box-shadow: 4px 4px 5px #df4375;
            background-color: #df4375;
            padding: 10px;
            margin-top: 100px;
        }

        input[type="text"],
        #select,
        #number {
            border: none;
            border-bottom: 1px solid whitesmoke;
            margin: 10px;
            padding: 10px;
            outline: none;
            background: transparent;
        }

        #navbar {
            background-color: #101820FF;
            color: white;

        }

        .nav-link {
            color: white;
            border-radius: 10px;
            margin: 10px;
        }

        .nav-link:hover {
            background-color: #f2AA4CFF;
            color: black;
        }

        .off {
            background-color: #101820FF;
        }

        button {
            background-color: #101820FF;
            color: white;
            border-radius: 10px;
            padding: 7px;
            width: 80px;
        }

        button:hover {
            background-color: #f2AA4CFF;
            color: black;
        }
    </style>
</head>

<body>
    <div>
        <div id="navbar"><%- include('./partials/header') %></div>
        <center>
            <div id="myform">
                <form action="/home/bookings/edit/<%= booking.name %>" method="post">
                    <label>Name :</label><input type="text" name="name" value="<%=booking.name %>"><br>
                    <label>Viechle :</label><input type="text" name="vehicle" value="<%=booking.viechle %>"><br>
                    <label>Service :</label>
                    <select name="service" id="select" value="<%=booking.service %>">
                        <% serviceDetails.forEach((details)=>{%>
                            <option>
                                <%=details.service %>
                            </option>
                            <%});%>
                    </select><br>
                    <label>Cantact :</label><input type="number" name="contact" id="number"
                        value="<%=booking.contact %>"><br>
                    <button type="submit">Update</button>
                </form>
            </div>
        </center>
    </div>
</body>

</html>